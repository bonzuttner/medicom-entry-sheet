# local (default): browser localStorage
VITE_DATA_SOURCE=local

# Runtime mode
# test: relaxed guardrails for test/staging
# production: strict security checks enabled
# APP_RUNTIME_ENV=test

# api: switch to backend API (for AWS migration)
# VITE_DATA_SOURCE=api
# VITE_API_BASE=http://localhost:3000

# Vercel KV (required for persistent API data on Vercel)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Vercel Blob (required for image/attachment offloading)
# BLOB_READ_WRITE_TOKEN=
# Optional: additional allowed media hostnames (comma-separated)
# MEDIA_ALLOWED_HOSTS=

# Session signing secret (required when APP_RUNTIME_ENV=production)
# SESSION_SECRET=
